import {NextResponse} from "next/server"; import {createClient} from "@/lib/supabase/server";
export async function POST(req:Request){const form=await req.formData();const supabase=await createClient();const payload={name:String(form.get("name")||""),phone:String(form.get("phone")||""),style_id:Number(form.get("style_id")),body_area:String(form.get("body_area")||""),size:String(form.get("size")||""),preferred_date:String(form.get("preferred_date")||"")||null,notes:String(form.get("notes")||"")};const {error}=await supabase.from("booking_requests").insert(payload);if(error)return new NextResponse("خطا در ثبت درخواست",{status:500});return NextResponse.redirect(new URL("/?sent=1#booking",req.url));}